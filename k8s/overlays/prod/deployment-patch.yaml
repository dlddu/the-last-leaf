apiVersion: apps/v1
kind: Deployment
metadata:
  name: the-last-leaf
spec:
  template:
    spec:
      containers:
      - name: the-last-leaf
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
