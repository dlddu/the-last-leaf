apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # secret.example.yaml is a template only.
  # Create your own secret.yaml (or use SealedSecrets / External Secrets) before deploying.
